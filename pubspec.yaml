name: expensesmanager
description: Simple to use android mobile application that helps you to manage your daily expenses & keep you on budget.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.24.1
  sqflite: ^1.3.2+1
  cupertino_icons: ^1.0.0
  line_awesome_flutter: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
